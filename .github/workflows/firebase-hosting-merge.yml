# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - name: Build project with VITE environment variable
        run: npm run build # Make sure you have a 'build' script in your package.json, e.g., "build": "vite build"
        env:
          VITE_SPOONACULAR_API_KEY: ${{ secrets.SPOONACULAR_API_KEY }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_RECIPE_FINDER_22742 }}
          channelId: live
          projectId: recipe-finder-22742
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
